% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty-core-v0.2.0.R,
%   R/uncertainty-core.R
\name{prn_build_uncertain}
\alias{prn_build_uncertain}
\title{Fit an uncertainty-aware Perturbation Response Network}
\usage{
prn_build_uncertain(
  fingerprint,
  prior,
  source_col = "source",
  target_col = "target",
  sign_col = "sign",
  confidence_col = "confidence",
  n_draws = 100L,
  min_targets = 10L,
  lambda = NULL,
  counter_threshold = 0,
  top_fraction = 0.1,
  seed = 1L,
  keep_draws = FALSE
)

prn_build_uncertain(
  fingerprint,
  prior,
  source_col = "source",
  target_col = "target",
  sign_col = "sign",
  confidence_col = "confidence",
  n_draws = 100L,
  min_targets = 10L,
  lambda = NULL,
  counter_threshold = 0,
  top_fraction = 0.1,
  seed = 1L,
  keep_draws = FALSE
)
}
\arguments{
\item{fingerprint}{Gene-by-comparison response matrix.}

\item{prior}{Directed edge table.}

\item{source_col, target_col, sign_col}{Prior column names.}

\item{confidence_col}{Column containing the probability that the reported
sign is correct. Values must lie in [0,1]. A value of 1 fixes the sign;
0.5 represents an uninformative sign; 0 means the opposite sign is known.}

\item{n_draws}{Number of plausible signed networks to sample.}

\item{min_targets}{Minimum targets per regulator passed to `prn_build()`.}

\item{lambda}{Ridge penalty passed to `prn_build()`.}

\item{counter_threshold}{Counterdirection magnitude counted as present.}

\item{top_fraction}{Fraction of targets called top candidates in each draw.}

\item{seed}{Random seed.}

\item{keep_draws}{Retain draw-level target results for diagnostics.}
}
\value{
A `prn_uncertain` object containing confidence-weighted and ensemble
  summaries.

A `prn_uncertain` object containing expected-sign and both
  unweighted and data-supported ensemble summaries.
}
\description{
Repeatedly samples plausible regulatory signs, fits one PRN per sampled
network, and summarizes which target-level counterdirection signals and
regulator activities remain stable.

Repeatedly samples plausible regulatory signs, fits one PRN per sampled
network, scores each sampled network by data support, and summarizes which
target-level counterdirection signals and regulator activities remain stable.
}
