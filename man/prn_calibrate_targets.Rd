% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prn-calibrate-targets.R
\name{prn_calibrate_targets}
\alias{prn_calibrate_targets}
\title{Degree-conditioned calibration of target counterdirection}
\usage{
prn_calibrate_targets(
  object,
  fingerprint,
  prior,
  source_col = "source",
  target_col = "target",
  sign_col = "sign",
  n_null = 100L,
  swaps_per_edge = 5,
  min_targets = 10L,
  lambda = NULL,
  seed = 1L,
  evidence_level = "degree_preserving_null"
)
}
\arguments{
\item{object}{A fitted `prn_fit` object returned by [prn_build()].}

\item{fingerprint}{Numeric gene-by-comparison response matrix used to fit
`object`, with gene row names and comparison column names.}

\item{prior}{Signed regulatory prior with source, target, and sign columns.}

\item{source_col, target_col, sign_col}{Column names in `prior`.}

\item{n_null}{Number of successful degree-preserving null fits.}

\item{swaps_per_edge}{Number of attempted double-edge swaps per retained
prior edge for each null draw.}

\item{min_targets}{Minimum observed targets per regulator, as in [prn_build()].}

\item{lambda}{Ridge penalty. By default, reuse the unique penalty recorded in
`object`; supply a value if the fit does not expose a unique penalty.}

\item{seed}{Random seed.}

\item{evidence_level}{Evidence label assigned to null fits.}
}
\value{
A list with `calibration`, `null_summary`, `draw_diagnostics`, and
  `settings`. `calibration` contains observed counterdirection, incoming
  degree, null mean and standard deviation, a degree-conditioned Z-score,
  null percentile, and an empirical upper-tail probability for every
  target-comparison pair.
}
\description{
Calibrate target-level counterdirection against bipartite network nulls that
preserve every regulator's out-degree and every target's in-degree. Edge
signs remain attached to their source-edge records during double-edge swaps,
preserving each regulator's sign counts.
}
